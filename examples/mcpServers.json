{
  "mcpServers": {
    "aws-s3": {
      "command": "node",
      "args": ["./dist/index.js"],
      "env": {
        "AWS_REGION": "us-east-1",
        "AWS_ACCESS_KEY_ID": "****",
        "AWS_SECRET_ACCESS_KEY": "****"
      },
      "autoApprove": ["list_buckets", "list_objects", "get_bucket_policy"],
      "disabled": false
    }
  }
}